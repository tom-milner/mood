import 'package:flutter/material.dart';

class ScenariosPage extends StatelessWidget {
  final Color color;

  ScenariosPage(this.color);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      color: color,
    );
  }
}