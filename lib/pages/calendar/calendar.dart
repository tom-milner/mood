import 'package:flutter/material.dart';

class CalendarPage extends StatelessWidget {
  final Color color;

  CalendarPage(this.color);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      color: color,
    );
  }
}